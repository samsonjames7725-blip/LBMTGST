# LBMTGST — LifeBridge MedTech GST/ERP
# Separate codebase from the AI Business OS (D:\LBMT / its own repo).
# Shares the SAME Supabase platform — never create a second database.

# --- Supabase (shared platform: ghvjdybgllufjtdurbvh) ---
# URL and publishable key are public by design; the secret key must never
# be exposed to the browser or committed.
NEXT_PUBLIC_APP_URL=
NEXT_PUBLIC_SUPABASE_URL=https://ghvjdybgllufjtdurbvh.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Server-only. Never expose to the browser, never commit.
SUPABASE_SECRET_KEY=

# Protects cron/automation endpoints (server-only; used from the
# automation stage onward)
CRON_SECRET=
